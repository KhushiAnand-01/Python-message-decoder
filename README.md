# Python-message-decoder
Python assignment to decode an encrypted message and print possible outputs. It takes an encrypted input, matches it with predefined codes, and generates valid decoded words.
